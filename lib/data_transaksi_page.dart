import 'package:flutter/material.dart';

class DataTransaksiPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text("Data Transaksi"),
      ),
      body: Center(
        child: Text("Halaman Data Transaksi"),
      ),
    );
  }
}
